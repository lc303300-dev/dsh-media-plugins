# Patch/diff files must stay byte-identical LF so they remain valid
# `git apply` input on any machine. `-text` (= no EOL conversion / autocrlf
# bypass) stores and checks them out verbatim; leave the file LF on disk.
*.patch -text
*.diff -text
