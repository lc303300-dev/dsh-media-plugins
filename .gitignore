# 依赖与构建
node_modules/
dist/
*.tgz
obj/

# Dreamina CLI 二进制（31.9MB），由 setup.ps1 从官方地址下载，不进仓库
bin/

# 本地凭证/环境，绝不提交
.env
*.credentials

# 验收/运行产物
outputs/

# 本地运行时状态（registry/项目/批次/日志/锁），与仓库无关
.dsh-media-private/
